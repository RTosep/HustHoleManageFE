import { makeStyles } from '@material-ui/core/styles';

const FeedBacksStyles = makeStyles(() => ({
  refreshBtn: {
    width: '94px',
    height: '36px',
    left: '1002px',
    top: '24px',
    color: '#4B9F79',
    position: 'absolute',
  },
}));
export default FeedBacksStyles;
