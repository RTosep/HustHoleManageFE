// import { CHANGE } from './actionType';

// export default function changeColor(state = 0, action) {
//   switch (action.type) {
//     case CHANGE: {
//       const newState = action.data;
//       return newState;
//     }
//     default:
//       return state;
//   }
// }
